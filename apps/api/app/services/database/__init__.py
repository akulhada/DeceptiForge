"""PostgreSQL database honey records: classification, suitability, generation, safety, and safe
connector deployment. Writes are least-privilege, approved, transactional, bounded, and reversible;
the default is to reject anything risky. Real production data is never read or modified."""
