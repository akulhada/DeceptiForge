"""AI-native tripwire sensors: RAG document + MCP resource decoys, safe content generation, trace
design that survives chunking/embedding, event data minimization, and the deploy/verify/retire
lifecycle. Deployed assets are inert and reversible; raw prompts/chunks/outputs are never stored."""
