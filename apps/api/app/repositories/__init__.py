# Purpose: reserve persistence access modules. Responsibilities: isolate non-trivial query logic
# from services. Future modules: add repositories only when direct SQLAlchemy use becomes
# repetitive.
