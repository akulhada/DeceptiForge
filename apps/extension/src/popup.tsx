// Purpose: define the browser-action popup entry point. Responsibilities: keep the Plasmo extension buildable before product interaction exists. Future modules: render the approved extension interface here.
export default function Popup() {
  return null;
}
