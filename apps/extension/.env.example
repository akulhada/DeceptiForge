# Purpose: template extension build-time configuration. Responsibilities: enumerate public variables available to bundled browser code. Future modules: add only public endpoint configuration; never embed credentials.
PLASMO_PUBLIC_API_URL=http://localhost:8000
