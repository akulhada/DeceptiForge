// Purpose: provide the required Next.js root route. Responsibilities: keep the application buildable before product UI exists. Future modules: replace with the authenticated product entry point.
export default function HomePage() {
  return null;
}
