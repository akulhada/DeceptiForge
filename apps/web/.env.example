# Purpose: template frontend build-time configuration. Responsibilities: enumerate public variables consumed by the browser application. Future modules: add public integration URLs only with their integration.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Demo controls are local-development-only and require an explicit opt-in.
NEXT_PUBLIC_DEMO_MODE=true
