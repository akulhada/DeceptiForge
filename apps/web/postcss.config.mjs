// Purpose: connect Tailwind to PostCSS. Responsibilities: compile Tailwind directives during Next.js builds. Future modules: add PostCSS plugins only for a demonstrated need.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
