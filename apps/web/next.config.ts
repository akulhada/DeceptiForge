// Purpose: define Next.js application configuration. Responsibilities: keep framework defaults explicit and configuration-free until a deployment need exists. Future modules: add security headers and image rules with their owning integration.
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {};

export default nextConfig;
